# Quotes-to-Scrape

Scraping:
Use Splinter to automate a browser and scrape data using BeautifulSoup.
BeautifulSoup tells which HTML to tag. Also, if it has an attribute such as specific class or id.

•	"Chrome is being controlled by automated test software.".
•	Python script is directing the browser and the browser will visit websites and interact with them on its own.
